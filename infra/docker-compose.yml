services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: cil
      POSTGRES_PASSWORD: cil
      POSTGRES_DB: cil
    ports: ["5432:5432"]
    volumes: ["cil_pg:/var/lib/postgresql/data"]
volumes:
  cil_pg:
