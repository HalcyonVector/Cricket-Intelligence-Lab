[project]
name = "cil-analytics"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["polars>=1.0", "numpy>=1.26", "scikit-learn>=1.4", "scipy>=1.11", "psycopg[binary]>=3.1"]
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
