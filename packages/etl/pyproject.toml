[project]
name = "cil-etl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["polars>=1.0", "psycopg[binary]>=3.1", "orjson>=3.9", "typer>=0.12"]
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
